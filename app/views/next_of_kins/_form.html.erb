<%= form_for(@next_of_kin) do |f| %>
  <% if @next_of_kin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@next_of_kin.errors.count, "error") %> prohibited this next_of_kin from being saved:</h2>

      <ul>
      <% @next_of_kin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :first_name %>
  </div>
  <div class="field">
    <%= f.input :last_name %>
  </div>
  <div class="field">
    <%= f.input :address %>
  </div>
  <div class="field">
    <%= f.input :contact_number %>
  </div>
  <div class="field">
    <%= f.input :relation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
